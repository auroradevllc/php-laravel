kind: pipeline
type: docker
name: default

steps:
  - name: docker
    image: techknowlogick/drone-docker
    settings:
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      repo: registry.meow.tf/tyler/php-laravel
      registry: registry.meow.tf
      tags:
        - php8